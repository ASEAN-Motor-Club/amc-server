name: Deploy Peripheral

on:
  push:
    branches: [master]
  workflow_dispatch: {}

concurrency:
  group: deploy-peripheral
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: [self-hosted, deploy-peripheral]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false
          persist-credentials: false

      - name: Deploy
        run: |
          # Trigger the root-owned self-deploy service (runs without
          # systemd namespace restrictions) and wait for it to complete.
          systemctl start amc-peripheral-deploy
          cat /var/lib/opencode/deploy-result.json

      - name: Health check
        run: |
          echo "Checking services..."
          for svc in amc-radio nginx; do
            if systemctl is-active --quiet "$svc" 2>/dev/null; then
              echo "  $svc: active"
            else
              echo "  $svc: not running"
              exit 1
            fi
          done
          echo "All services healthy"
